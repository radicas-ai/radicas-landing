/** Canonical, production URL of the site — single source of truth. */
export const siteUrl = "https://www.radicas.ai";

/** What the SERP and every share card show, kept here so the three copies in the metadata
    cannot drift apart. The brand leads because a browser tab truncates from the right. */
export const siteTitle = "Radicas · AI Cost Management and Governance";
export const siteDescription =
  "Humans and AI, one workforce. Radicas shows what the work costs, keeps it governed and proves what it returns.";
